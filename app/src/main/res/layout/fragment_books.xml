<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <io.legado.app.ui.widget.recycler.scroller.FastScrollRecyclerView
                android:id="@+id/rv_bookshelf"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingTop="0dp"
                android:paddingBottom="0dp"
                android:paddingLeft="8dp"
                android:paddingRight="8dp" />

            <androidx.compose.ui.platform.ComposeView
                android:id="@+id/compose_bookshelf"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone" />

        </FrameLayout>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <TextView
        android:id="@+id/tv_empty_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="16dp"
        android:gravity="center"
        android:text="@string/bookshelf_empty"
        android:visibility="gone"
        tools:text="TextView" />

    <!-- 雪帝主题装饰：书架头部淡雪帝 + 雪花（视觉水印，不拦截点击） -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false"
        android:importantForAccessibility="no">

        <!-- 左上小雪帝头像：小尺寸，透明度低，不挡搜索/顶栏内容 -->
        <ImageView
            android:id="@+id/xuedi_shelf_decor_icon"
            android:layout_width="46dp"
            android:layout_height="46dp"
            android:layout_gravity="top|start"
            android:layout_marginStart="6dp"
            android:layout_marginTop="4dp"
            android:alpha="0.34"
            android:contentDescription="@null"
            android:src="@drawable/xuedi_decor"
            tools:ignore="ContentDescription" />

        <!-- 右上雪花（classic 经典六角雪花） -->
        <ImageView
            android:id="@+id/xuedi_shelf_snow_tl"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="10dp"
            android:alpha="0.18"
            android:contentDescription="@null"
            android:src="@drawable/snowflake_classic"
            tools:ignore="ContentDescription" />

        <!-- 右下小雪花（branch 枝型） -->
        <ImageView
            android:id="@+id/xuedi_shelf_snow_br"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="26dp"
            android:alpha="0.15"
            android:contentDescription="@null"
            android:rotation="15"
            android:src="@drawable/snowflake_branch"
            tools:ignore="ContentDescription" />

    </FrameLayout>

</FrameLayout>

